<VirtualHost *:80>
  ServerName archive.my.domain:80

  DocumentRoot /a/www

  <Directory /a/www>
    AllowOverride All
  </Directory>

  RewriteEngine On
  Include /a/etc/archive.rewrite.conf

</VirtualHost>